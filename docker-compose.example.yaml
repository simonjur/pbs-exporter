services:
  pbs-exporter:
    image: ghcr.io/simonjur/pbs-exporter-node:alpha
    build:
        context: .
        dockerfile: Dockerfile
    container_name: pbs-exporter
    user: '65534'
    restart: always
    ports:
      - "10019:10019"
    environment:
      - PBS_API_TOKEN=
      - PBS_ENDPOINT=https://127.0.0.1:8007
      - PBS_LOGLEVEL=debug
      - PBS_USERNAME=root@pam
      - PBS_API_TOKEN_NAME=pbs-exporter
      - PBS_INSECURE=true
      - PBS_TIMEOUT=10s
